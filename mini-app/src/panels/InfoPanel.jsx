import { Panel, PanelHeader, PanelHeaderBack, Div } from '@vkontakte/vkui';
import { useRouteNavigator } from '@vkontakte/vk-mini-apps-router';
import PropTypes from 'prop-types';

const CONTENT = {
  ml: {
    title: 'Классификатор',
    kicker: 'Определение типа документа и режима проверки',
    lead: 'Классификатор нужен, чтобы пользователь не выбирал тип работы вручную. Приложение читает DOCX, ищет признаки титульного листа и структуры, определяет класс документа и автоматически подбирает профиль проверки: LIGHT, MEDIUM или HARD.',
    cards: [
      {
        title: 'Что извлекается из DOCX',
        text: 'Модуль app/ml/features.py читает документ через python-docx и считает признаки, которые устойчиво доступны без ручной разметки.',
        items: ['условное число страниц', 'глубина заголовков 1 / 1.1 / 1.1.1', 'наличие содержания, введения, заключения, реферата', 'количество таблиц, рисунков, формул, источников и приложений', 'корректность полей, объем текста и средняя длина абзаца'],
      },
      {
        title: 'Какие классы поддерживаются',
        text: 'Модель классифицирует 8 типов учебных и научных документов. От типа зависит рекомендуемый режим проверки.',
        items: ['lab_work и practice -> LIGHT', 'internship и coursework -> MEDIUM', 'thesis_bachelor и thesis_master -> HARD', 'scientific_rpt и rnd_nir -> HARD'],
      },
      {
        title: 'Как обучается модель',
        text: 'Скрипты scripts/extract_features.py и scripts/train_classifier.py формируют dataset.csv и обучают RandomForestClassifier. Стартовая bootstrap-модель нужна только для первого запуска.',
        items: ['минимум 50-100 DOCX на каждый класс', 'macro F1 на тесте не ниже 0.85', 'ни один класс не ниже F1 0.70', '5-fold macro F1 не ниже 0.82'],
      },
    ],
    flow: ['DOCX', 'features.py', '16 признаков', 'RandomForest', 'doc_type', 'mode'],
  },
  gost: {
    title: 'ГОСТ',
    kicker: 'Что именно проверяет валидатор',
    lead: 'Проверка устроена как конвейер: сначала документ разбирается на абзацы, таблицы, рисунки, формулы, листинги и списки, затем для выбранного профиля запускаются независимые checker-модули. Каждый checker возвращает код нарушения, ожидаемое значение, фактическое значение и фрагмент текста, чтобы замечание можно было показать в дашборде без ручного поиска по документу.',
    paper: true,
    cards: [
      {
        title: 'Базовое оформление',
        text: 'Этот блок отвечает за то, чтобы обычный текст был похож на оформленный отчет, а не на набор абзацев из разных шаблонов Word.',
        items: ['поля страницы проверяются отдельным page-модулем', 'шрифт, размер, начертание и интервалы проверяются font-модулями', 'абзацный отступ и выравнивание контролируются как часть форматирования текста', 'нумерация страниц проверяется в нижнем колонтитуле', 'титульный лист отдельно защищается от лишней видимой нумерации'],
      },
      {
        title: 'Объекты документа',
        text: 'Для таблиц, рисунков, формул и листингов используются отдельные правила, потому что у каждого такого элемента своя логика оформления.',
        items: ['подписи таблиц проверяются на формат и расположение', 'рисунки проверяются по подписи, пустой строке, центровке и отсутствию точки в конце подписи', 'формулы валидируются по выравниванию и номеру в скобках', 'листинги определяются осторожной эвристикой, чтобы обычный текст не считался кодом', 'списки проверяются по маркерам, нумерации и визуальному форматированию номера'],
      },
      {
        title: 'Профили проверки',
        text: 'Профиль выбирается автоматически после определения типа файла. Пользователь может перейти в CUSTOM, но тип документа вручную выбирать не нужно.',
        items: ['LIGHT подходит для лабораторных, практических и коротких файлов', 'MEDIUM добавляет правила для объектов и списков', 'HARD включает полный набор для ВКР, дипломов, НИР и крупных отчетов', 'если документ не распознан, он остается просто файлом, а проверка идет по безопасному профилю', 'нерелевантные правила отключаются по типу документа, чтобы не шуметь лишними ошибками'],
      },
      {
        title: 'Как читается результат',
        text: 'Дашборд показывает не абстрактный вердикт, а список конкретных замечаний, по которым потом можно запускать корректировку.',
        items: ['процент считается от количества найденных нарушений', 'частые коды группируются отдельно', 'каждая строка показывает ожидаемое и фактическое значение', 'warning не блокирует скачивание и нужен для спорных случаев', 'исправление запускается отдельно, чтобы валидация не меняла файл без действия пользователя'],
      },
    ],
  },
  autofix: {
    title: 'Автофикс',
    kicker: 'Как работает корректировка отчета',
    lead: 'Автофикс не переписывает смысловой текст. Он заново прогоняет документ через те же правила, применяет только подключенные fixer-модули к оформлению DOCX и возвращает исправленный файл. Поэтому пользователь сначала видит ошибки на дашборде, а уже потом явно запускает корректировку.',
    cards: [
      {
        title: 'Что исправляется',
        text: 'Коррекция разбита на модули. Каждый модуль отвечает за узкую область, поэтому одну часть алгоритма можно доработать, не ломая остальные.',
        items: ['MarginsFixer выставляет поля страницы по целевому профилю', 'PageNumberingFixer добавляет PAGE-нумерацию в нижний колонтитул и центрирует ее', 'FontFormatFixer приводит основной текст к Times New Roman, нужному размеру, интервалам и абзацному отступу', 'FigureCaptionFixer нормализует подписи рисунков: «Рисунок N — Название» без точки в конце', 'FigureLayoutFixer добавляет пустую строку перед рисунком и центрирует строку рисунка, пустую строку и подпись', 'ListMarkerFixer исправляет маркеры и формат нумерованных списков'],
      },
      {
        title: 'Как идет сценарий',
        text: 'Фронтенд ведет пользователя по короткому маршруту, где каждое действие понятно и обратимо.',
        items: ['Валидация: файл проверяется без изменений', 'Дашборд: показываются процент, частые коды и полный список замечаний', 'Корректировка: отдельная кнопка запускает исправление', 'Фиксеры: backend применяет только доступные модули оформления', 'Предпросмотр: строится страничный preview исправленного DOCX', 'Скачивание: пользователь забирает готовый *_fixed.docx'],
      },
      {
        title: 'Что не исправляется автоматически',
        text: 'Автофикс сознательно не делает правки, где нужен автор или нормоконтролер. Такие места остаются замечаниями в отчете.',
        items: ['не пишет отсутствующие разделы и выводы', 'не перепридумывает названия рисунков, таблиц и формул', 'не чинит смысловые ссылки на источники', 'не перестраивает сложные таблицы вручную', 'не исправляет библиографию, если нужна содержательная проверка источника', 'не трогает неоднозначные warning-ситуации'],
      },
      {
        title: 'Будущая история',
        text: 'Когда появится история проверок, ее лучше добавить отдельным слоем, чтобы текущая валидация и исправление остались простыми.',
        items: ['сохранять vk_user_id из launch-параметров', 'записывать имя файла, профиль и выбранные custom-правила', 'хранить процент до и после исправления', 'держать путь к original/fixed DOCX отдельно от отчета', 'удалять старые файлы по TTL', 'показывать пользователю последние проверки без повторной загрузки файла'],
      },
    ],
    flow: ['Валидация', 'Дашборд', 'Коррекция', 'Фиксеры', 'Предпросмотр', 'Скачивание'],
  },
};

function Flow({ items }) {
  if (!items?.length) return null;

  return (
    <div className="info-flow">
      {items.map((item, index) => (
        <div className="info-flow__node" key={item}>
          <span>{item}</span>
          {index < items.length - 1 && <b />}
        </div>
      ))}
    </div>
  );
}

export const InfoPanel = ({ id, type }) => {
  const routeNavigator = useRouteNavigator();
  const content = CONTENT[type];

  return (
    <Panel id={id}>
      <PanelHeader before={<PanelHeaderBack onClick={() => routeNavigator.push('/')} />}>
        {content.title}
      </PanelHeader>
      <Div>
        <section className={`info-page ${content.paper ? 'info-page--paper' : ''}`}>
          <div className="info-page__hero">
            <span>{content.kicker}</span>
            <h1>{content.title}</h1>
            <p>{content.lead}</p>
          </div>

          <Flow items={content.flow} />

          <div className="info-page__grid">
            {content.cards.map((card) => (
              <article className="info-card" key={card.title}>
                <h2>{card.title}</h2>
                <p>{card.text}</p>
                <ul>
                  {card.items.map((item) => (
                    <li key={item}>{item}</li>
                  ))}
                </ul>
              </article>
            ))}
          </div>
        </section>
      </Div>
    </Panel>
  );
};

Flow.propTypes = {
  items: PropTypes.arrayOf(PropTypes.string),
};

InfoPanel.propTypes = {
  id: PropTypes.string.isRequired,
  type: PropTypes.oneOf(['ml', 'gost', 'autofix']).isRequired,
};
